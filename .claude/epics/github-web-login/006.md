---
id: 006
title: Error Handling - 에러 처리 + 사용자 친화적 메시지
epic: github-web-login
status: backlog
priority: medium
created: 2025-09-06T13:02:56Z
updated: 2025-09-06T13:12:27Z
assignee: captain
effort: S
dependencies: [002, 004, 005]
parallel: []
tags: [frontend, backend, error-handling, ux]
github: https://github.com/JokerTrickster/workflow/issues/7
---

# Task 006: Error Handling - 에러 처리 + 사용자 친화적 메시지

## Overview

전체 GitHub 로그인 시스템에서 발생할 수 있는 다양한 에러 상황을 체계적으로 처리하고, 사용자에게 친화적이고 실행 가능한 메시지를 제공하는 통합 에러 처리 시스템을 구현합니다. 클라이언트와 서버 양쪽의 에러를 포괄하며, 자동 복구 메커니즘과 사용자 가이던스를 포함합니다.

## Technical Requirements

### Error Classification System
```typescript
enum ErrorCategory {
  AUTHENTICATION = 'auth',
  NETWORK = 'network',
  API_LIMIT = 'api_limit',
  PERMISSION = 'permission',
  VALIDATION = 'validation',
  SERVER = 'server',
  CLIENT = 'client',
}

enum ErrorSeverity {
  LOW = 'low',           // 사용자 액션으로 해결 가능
  MEDIUM = 'medium',     // 재시도로 해결 가능
  HIGH = 'high',         // 시스템 개입 필요
  CRITICAL = 'critical', // 서비스 중단
}

interface AppError {
  id: string;
  category: ErrorCategory;
  severity: ErrorSeverity;
  code: string;
  message: string;
  userMessage: string;
  suggestions: string[];
  retryable: boolean;
  timestamp: Date;
  context?: Record<string, any>;
}
```

### Error Scenarios & Handling Strategy

#### Authentication Errors
```typescript
const authErrors = {
  SUPABASE_SESSION_EXPIRED: {
    userMessage: "세션이 만료되었습니다. 다시 로그인해주세요.",
    suggestions: ["로그인 페이지로 이동", "자동 리다이렉션 대기"],
    recovery: "자동 로그아웃 후 로그인 페이지 리다이렉션",
  },
  GITHUB_TOKEN_INVALID: {
    userMessage: "GitHub 연동이 해제되었습니다. 다시 연결해주세요.",
    suggestions: ["GitHub 재연결", "권한 설정 확인"],
    recovery: "OAuth 재인증 플로우 시작",
  },
  OAUTH_FLOW_INTERRUPTED: {
    userMessage: "로그인 과정에서 오류가 발생했습니다. 다시 시도해주세요.",
    suggestions: ["로그인 재시도", "브라우저 캐시 삭제"],
    recovery: "OAuth 상태 초기화 후 재시작",
  }
};
```

#### API & Network Errors
```typescript
const apiErrors = {
  GITHUB_RATE_LIMIT: {
    userMessage: "GitHub API 사용량이 초과되었습니다. 잠시 후 다시 시도해주세요.",
    suggestions: [`${resetTime}에 다시 시도`, "캐시된 데이터 사용"],
    recovery: "Rate limit reset까지 대기, 캐시 데이터 표시",
  },
  NETWORK_TIMEOUT: {
    userMessage: "네트워크 연결이 느립니다. 다시 시도하시겠습니까?",
    suggestions: ["재시도", "네트워크 연결 확인"],
    recovery: "지수 백오프로 자동 재시도 (3회)",
  },
  SERVER_ERROR: {
    userMessage: "일시적인 서버 오류입니다. 잠시 후 다시 시도해주세요.",
    suggestions: ["페이지 새로고침", "몇 분 후 재시도"],
    recovery: "자동 재시도 후 오류 보고 전송",
  }
};
```

## Implementation Details

### File Structure
```
src/lib/errors/
├── error-types.ts - 에러 타입 정의
├── error-handler.ts - 글로벌 에러 핸들러
├── error-recovery.ts - 자동 복구 로직
└── error-reporting.ts - 에러 리포팅 (선택사항)

src/components/errors/
├── ErrorBoundary.tsx - React Error Boundary
├── ErrorDialog.tsx - 에러 다이얼로그 컴포넌트
├── ErrorToast.tsx - 토스트 알림 컴포넌트
└── NetworkErrorBanner.tsx - 네트워크 상태 배너

src/hooks/
├── useErrorHandler.ts - 에러 처리 훅
├── useNetworkStatus.ts - 네트워크 상태 감지
└── useRetry.ts - 재시도 로직 훅
```

### Global Error Handler
```typescript
class GlobalErrorHandler {
  private errorQueue: AppError[] = [];
  private retryQueue: Map<string, RetryConfig> = new Map();

  handle(error: Error, context?: ErrorContext): AppError {
    const appError = this.classifyError(error, context);
    
    // 자동 복구 시도
    if (appError.retryable) {
      this.scheduleRetry(appError);
    }
    
    // 사용자 알림
    this.notifyUser(appError);
    
    // 에러 로깅
    this.logError(appError);
    
    return appError;
  }

  private async scheduleRetry(error: AppError) {
    const config = this.getRetryConfig(error.category);
    const retryId = `${error.category}_${Date.now()}`;
    
    this.retryQueue.set(retryId, {
      error,
      attempts: 0,
      maxAttempts: config.maxAttempts,
      backoffMs: config.initialBackoff,
    });
    
    setTimeout(() => this.executeRetry(retryId), config.initialBackoff);
  }
}
```

### React Error Boundary
```typescript
interface ErrorBoundaryState {
  hasError: boolean;
  error: AppError | null;
  errorId: string | null;
}

class AppErrorBoundary extends Component<PropsWithChildren, ErrorBoundaryState> {
  state: ErrorBoundaryState = {
    hasError: false,
    error: null,
    errorId: null,
  };

  static getDerivedStateFromError(error: Error): ErrorBoundaryState {
    const appError = globalErrorHandler.handle(error, { boundary: true });
    return {
      hasError: true,
      error: appError,
      errorId: appError.id,
    };
  }

  componentDidCatch(error: Error, errorInfo: ErrorInfo) {
    // Additional error reporting
    console.error('Error Boundary caught an error:', { error, errorInfo });
  }

  render() {
    if (this.state.hasError) {
      return (
        <ErrorFallback
          error={this.state.error}
          resetErrorBoundary={() => this.setState({ hasError: false, error: null })}
        />
      );
    }

    return this.props.children;
  }
}
```

### User-Friendly Error Components
```typescript
interface ErrorDialogProps {
  error: AppError;
  onRetry?: () => Promise<void>;
  onDismiss?: () => void;
  open: boolean;
}

export function ErrorDialog({ error, onRetry, onDismiss, open }: ErrorDialogProps) {
  return (
    <Dialog open={open} onOpenChange={onDismiss}>
      <DialogContent className="max-w-md">
        <DialogHeader>
          <DialogTitle className="flex items-center gap-2">
            <AlertCircle className="h-5 w-5 text-red-500" />
            {getErrorTitle(error.category)}
          </DialogTitle>
        </DialogHeader>
        
        <div className="space-y-4">
          <p className="text-sm text-gray-600">{error.userMessage}</p>
          
          {error.suggestions.length > 0 && (
            <div>
              <p className="text-sm font-medium mb-2">해결 방법:</p>
              <ul className="text-sm space-y-1">
                {error.suggestions.map((suggestion, idx) => (
                  <li key={idx} className="flex items-start gap-2">
                    <span>•</span>
                    <span>{suggestion}</span>
                  </li>
                ))}
              </ul>
            </div>
          )}
        </div>

        <DialogFooter>
          {onRetry && error.retryable && (
            <Button onClick={onRetry} disabled={retrying}>
              {retrying ? "재시도 중..." : "다시 시도"}
            </Button>
          )}
          <Button variant="outline" onClick={onDismiss}>
            닫기
          </Button>
        </DialogFooter>
      </DialogContent>
    </Dialog>
  );
}
```

## Acceptance Criteria

### Functional Requirements
- [ ] 모든 주요 에러 시나리오 식별 및 처리 구현
- [ ] 사용자 친화적 에러 메시지 및 해결 가이드 제공
- [ ] 자동 재시도 로직 (네트워크 오류, Rate limit 등)
- [ ] Error Boundary로 치명적 에러 격리
- [ ] 네트워크 상태 실시간 모니터링

### UX Requirements
- [ ] 에러 메시지 한국어 현지화
- [ ] 에러별 적절한 아이콘 및 색상 구분
- [ ] 토스트/다이얼로그/배너 등 상황별 적절한 UI
- [ ] 로딩 중 상태와 에러 상태 명확한 구분
- [ ] 에러 해결을 위한 명확한 CTA (Call-to-Action)

### Recovery Requirements
- [ ] OAuth 에러 시 자동 로그아웃 후 재로그인 유도
- [ ] Rate limit 시 reset 시간까지 대기 후 자동 재시도
- [ ] 네트워크 에러 시 지수 백오프로 재시도 (1초, 2초, 4초)
- [ ] 복구 불가능한 에러는 사용자에게 명확한 안내

### Monitoring Requirements
- [ ] 에러 발생률 모니터링 (에러 유형별)
- [ ] 에러 로그 수집 (클라이언트 + 서버)
- [ ] 사용자 피드백 수집 메커니즘
- [ ] 에러 복구 성공률 추적

## Testing Strategy

### Error Simulation Tests
```typescript
describe('Error Handling', () => {
  test('should handle Supabase session expiry');
  test('should handle GitHub rate limit with countdown');
  test('should handle network timeout with retry');
  test('should display user-friendly messages');
  test('should execute automatic recovery flows');
});

describe('Error Boundary', () => {
  test('should catch component rendering errors');
  test('should display fallback UI');
  test('should allow error boundary reset');
  test('should report errors to monitoring service');
});
```

### Integration Tests
- 실제 API 에러 시나리오 테스트
- OAuth 인터럽트 시나리오
- 네트워크 단절 시나리오
- Error recovery 플로우 전체 테스트

### Manual Test Scenarios
- 네트워크 연결 끊기/재연결
- GitHub OAuth 권한 취소
- API Rate limit 의도적 초과
- 비정상적인 브라우저 종료 후 재접속

## Dependencies

### Prerequisites
- Task 002: OAuth 인증 플로우 (인증 에러 처리)
- Task 004: GitHub API 연동 (API 에러 처리)
- Task 005: Dashboard UI (UI 에러 상태 처리)

### External Dependencies
```json
{
  "react-error-boundary": "^4.0.0",
  "@radix-ui/react-dialog": "^1.0.0",
  "@radix-ui/react-toast": "^1.0.0",
  "lucide-react": "^0.400.0"
}
```

### Internal Dependencies
- Global error handling context
- Toast notification system
- Retry mechanism utilities
- Network status detection

## Risk Mitigation

### Technical Risks
- **에러 핸들링 자체 에러**: Try-catch로 에러 핸들러 보호
- **무한 재시도**: 최대 재시도 횟수 및 백오프 제한
- **메모리 누수**: 에러 큐 및 타이머 정리 로직

### UX Risks
- **에러 피로**: 동일 에러 반복 표시 방지
- **사용자 혼란**: 기술적 용어 제거, 명확한 액션 가이드
- **접근성**: 스크린 리더 지원, 키보드 네비게이션

## Definition of Done

- [ ] 에러 분류 체계 및 타입 정의 완료
- [ ] 글로벌 에러 핸들러 구현 및 통합
- [ ] React Error Boundary 구현 및 적용
- [ ] 사용자 친화적 에러 UI 컴포넌트 완성
- [ ] 자동 재시도 및 복구 로직 구현
- [ ] 에러 상황별 사용자 메시지 한국어화 완료
- [ ] 에러 처리 통합 테스트 작성 및 통과
- [ ] 네트워크 상태 모니터링 구현
- [ ] 에러 로깅 및 모니터링 연동
- [ ] 코드 리뷰 완료 및 문서화

## Notes

### Error Message Guidelines
- 사용자 관점에서 이해하기 쉬운 언어 사용
- 구체적인 해결 방법 제시
- 기술적 세부사항 숨김 (개발자 콘솔에만 표시)
- 긍정적이고 도움이 되는 톤 유지

### Performance Considerations
- 에러 처리 로직이 메인 플로우 성능에 영향 없도록
- 에러 로깅은 비동기로 처리
- 재시도 로직의 백오프 타이밍 최적화

### Future Enhancements
- 에러 분석 대시보드 구축
- A/B 테스트를 통한 에러 메시지 최적화
- 사용자 에러 피드백 수집 시스템
- 예측적 에러 방지 메커니즘